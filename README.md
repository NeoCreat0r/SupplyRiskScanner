
## 📌 О проекте

**SupplyRiskScanner** - это быстрый, консольный инструмент для пентестеров, аналитиков безопасности и разработчиков, позволяющий:

- Сканировать зависимости из **PyPI** и **NuGet**
- Получать CVE через **официальный NVD API**
- Анализировать метаданные пакетов:
  - кол-во версий,
  - дата последнего релиза,
  - репозиторий,
  - активность разработчиков
- Генерировать **красивые HTML/JSON отчёты**
- Работать в виде **одного единственного .exe**, без .NET на хосте

Полностью локальный инструмент - **никаких сторонних SaaS**.

---

## 🚀 Возможности

| Возможность | Подробности |
|------------|-------------|
| 🔎 PyPI анализ | Метаданные, версии, репозиторий, активность |
| 📦 NuGet анализ | Полноценное получение данных о пакете |
| 🛡 CVE интеграция | NVD API → CVSS → описание → ссылки |
| 📊 HTML отчёт | Красивый, структурированный, читабельный |
| 📁 JSON отчёт | Формат для CI, SIEM, SAST |
| 🧩 Оценка риска | Score engine + уровни риска |
| 🧨 Single-File EXE | Полная автономность |

---

## 📥 Установка
Скачайте готовый EXE из релизов `SupplyRiskScanner.exe`.

Программа работает сразу - не нужен Python/.NET/pip/nuget.

## 🧪 Пример использования

### Сканировать Python `requirements.txt`
```bash
SupplyRiskScanner.exe --path "./requirements.txt" --ecosystems "pypi" --out report.html
```
### Сканировать C# проект и зависимость `NuGet`
```bash
SupplyRiskScanner.exe --projects "./MyProject" --ecosystems "nuget" --out risk.jso
```
### Смешанная проверка (PyPI + NuGet)
```bash
SupplyRiskScanner.exe --path "./" --ecosystems "pypi,nuget" --out full-report.html
```
